version: 2
sql:
  - engine: "sqlite"
    schema:
      - "schema.sql"
    queries:
      - "server.sql"
    gen:
      go:
        emit_exact_table_names: true
        package: "db"
        out: "../../db"
